#include <iostream>
#include <string>
#include "UListaP.h"
using namespace std;


int main(){
	ListaP* list;
	list=new ListaP();
	list->inserta_primero(10);
	list->inserta_ultimo(20);
	list->inserta_ultimo(30);
 	cout<<list->to_str()<<endl;

system("pause");
return 0;
}
